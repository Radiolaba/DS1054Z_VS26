clc;
clear;

% Читаем CSV: 3 столбца (V1, V2, t), разделитель — ';'
M = dlmread('data.csv',';');
t = M(:,3);

% Вычисляем и выводим частоту дискретизации
Fs = 1/mean(diff(t));
fprintf('Частота дискретизации: %.2f Гц\n', Fs);